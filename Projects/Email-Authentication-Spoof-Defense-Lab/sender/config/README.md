# Sender configuration

The source README references this directory for docker-mailserver configuration and generated DKIM keys, but it does not provide the complete file contents. Copy the actual lab configuration here. Never commit private DKIM keys.
